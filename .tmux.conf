# basic
set-option -g mouse on

set -g history-limit 2000
setw -g mode-keys vi

setw -g automatic-rename off
setw -g allow-rename off

set -g default-terminal tmux-256color
set-option -ga terminal-overrides ",*256col*:Tc"

if-shell 'tmux -V | grep -Eq "3\.[3-9]|[4-9]\."' "set -g allow-passthrough on"
set -ga update-environment TERM
set -ga update-environment TERM_PROGRAM

# keymap
bind -r ^h select-pane -L
bind -r ^j select-pane -D
bind -r ^k select-pane -U
bind -r ^l select-pane -R

bind -r - resize-pane -U 5
bind -r + resize-pane -D 5
bind -r < resize-pane -L 5
bind -r > resize-pane -R 5

bind '"' split-window -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"

## popup window
if-shell -b 'tmux -V | grep -qE "^tmux ([4-9]|3\.([2-9]|[1-9][0-9]))"' {
    bind-key T display-popup -E "$SHELL -l"
    bind-key P display-popup -w 80% -h 80% -E "$SHELL -l"
    bind-key H display-popup -w 80% -h 80% -E "btop"
    bind-key F display-popup -w 80% -h 80% -E "joshuto"
}

bind-key S setw synchronize-panes
bind-key R source-file ~/.tmux.conf \; display-message "tmux config reloaded"

bind-key -n C-M-enter select-layout main-vertical \; resize-pane -t 0 -x 120
bind-key -n C-M-space select-layout tiled
bind-key -n C-M-j next-layout
bind-key -n C-M-k previous-layout

is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|l?n?vim?x?|fzf)(diff)?$'"
bind-key -n 'C-h' if-shell "$is_vim" { send-keys C-h } { if-shell -F '#{pane_at_left}'   {} { select-pane -L } }
bind-key -n 'C-j' if-shell "$is_vim" { send-keys C-j } { if-shell -F '#{pane_at_bottom}' {} { select-pane -D } }
bind-key -n 'C-k' if-shell "$is_vim" { send-keys C-k } { if-shell -F '#{pane_at_top}'    {} { select-pane -U } }
bind-key -n 'C-l' if-shell "$is_vim" { send-keys C-l } { if-shell -F '#{pane_at_right}'  {} { select-pane -R } }

bind-key -T copy-mode-vi 'C-h' if-shell -F '#{pane_at_left}'   {} { select-pane -L }
bind-key -T copy-mode-vi 'C-j' if-shell -F '#{pane_at_bottom}' {} { select-pane -D }
bind-key -T copy-mode-vi 'C-k' if-shell -F '#{pane_at_top}'    {} { select-pane -U }
bind-key -T copy-mode-vi 'C-l' if-shell -F '#{pane_at_right}'  {} { select-pane -R }

if-shell "command -v xclip >/dev/null 2>&1" \
  "bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel 'xclip -selection clipboard'; \
   bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'xclip -selection clipboard'; \
   bind-key -T copy-mode-vi Enter send-keys -X copy-pipe-and-cancel 'xclip -selection clipboard'" \
  ""

bind -T copy-mode-vi r send-keys -X copy-pipe-and-cancel "tmux_open_vim"

# ui
## theme color
if-shell -F '#{IS_SSH}' {
    set -g @background      "#24273a"
    set -g @background-alt  "#494d64"
    set -g @foreground      "#cad3f5"
    set -g @foreground-alt  "#181926"
    set -g @active          "#c6a0f6"
    set -g @selected        "#c6a0f6"
} {
    set -g @background      "#273746"
    set -g @background-alt  "#46475a"
    set -g @foreground      "white"
    set -g @foreground-alt  "black"
    set -g @active          "colour31"
    set -g @selected        "#08C9D2"
}

## style
set -gF status-fg '#{@foreground}'
set -gF status-bg '#{@background}'
set -g mode-style "fg=#{@foreground-alt},bg=#{@active},bold"

## left down
set-option -g status-left '#{?IS_SSH,#[fg=#{@foreground}][server],}#{prefix_highlight}#[fg=#{@foreground-alt},bg=#{@active}] #S '
set-option -g status-left-length 20

## window list
set-option -g status-justify centre # 居中显示
set-window-option -g window-status-format         "#[fg=#{@foreground},bg=#{@background-alt}] #I #W #{window_flags} "
set-window-option -g window-status-current-format "#[fg=#{@foreground-alt},bg=#{@active}] #I #W #{window_flags} "

## border style
set -g pane-border-style        "fg=#{@background-alt}"
set -g pane-active-border-style "fg=#{@selected}"

if-shell -b 'tmux -V | grep -qE "^tmux ([3-9]|2\.([6-9]|[1-9][0-9]))"' \
  'set -g pane-border-status top; set -g pane-border-format " #[bold]#P #[nobold]#{pane_current_command} "'
if-shell -b 'tmux -V | grep -qE "^tmux ([4-9]|3\.([2-9]|[1-9][0-9]))"' \
  'set -g pane-border-lines heavy'

# plugin
CUSTOM_PLUGINS_DIR='~/.config/tmux/plugins/'
set-environment -g TMUX_PLUGIN_MANAGER_PATH $CUSTOM_PLUGINS_DIR

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-prefix-highlight'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-open'
set -g @plugin 'tmux-plugins/tmux-copycat'
set -g @plugin 'tmux-plugins/tmux-cowboy'
set -g @plugin 'laktak/extrakto'
set -g @plugin 'Morantron/tmux-fingers'
set -g @plugin 'omerxx/tmux-floax'

# tmux-floax
set -g @floax-bind '-n C-M-o'

# tmux-fingers
set -g @fingers-key space

# prefix_highlight
set -g @prefix_highlight_prefix_prompt 'Wait'
set -g @prefix_highlight_fg 'yellow'
set -g @prefix_highlight_bg '#273746'
set -g @prefix_highlight_show_copy_mode 'on'
set -g @prefix_highlight_copy_mode_attr 'fg=black,bg=green'
set -g @prefix_highlight_show_sync_mode 'on'
set -g @prefix_highlight_sync_mode_attr 'fg=black,bg=green'
set -g @prefix_highlight_empty_has_affixes 'on'
set -g @prefix_highlight_empty_prompt 'Tmux'

run '~/.config/tmux/plugins/tpm/tpm'
