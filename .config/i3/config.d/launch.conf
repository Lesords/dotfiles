# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# file manager
bindsym $mod+e exec thunar

# start rofi (a program launcher)
bindsym $mod+n exec --no-startup-id rofi -show drun   -theme /home/$USER/.config/rofi/mine.rasi
bindsym $mod+o exec --no-startup-id rofi -show window -theme /home/$USER/.config/rofi/windows.rasi
bindsym $mod+y exec --no-startup-id rofi -show clipboard -run-command '{cmd}' \
                    -modi "clipboard:greenclip print" -theme /home/$USER/.config/rofi/clip.rasi
bindsym $mod+p exec --no-startup-id /home/$USER/.config/i3/scripts/rofi-powermenu
bindsym $mod+m exec --no-startup-id /home/$USER/.config/i3/scripts/rofi-session

# scroll up
bindsym --whole-window $mod+button4 exec --no-startup-id rofi -show drun   -theme /home/$USER/.config/rofi/mine.rasi
# scroll down
bindsym --whole-window $mod+button5 exec --no-startup-id rofi -show window -theme /home/$USER/.config/rofi/windows.rasi

# flameshot
bindsym --release $mod+Shift+s exec flameshot gui
bindsym --release Print exec flameshot full --clipboard
